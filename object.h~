#pragma once

#include <iostream>
#include <vector>
#include <string>
#include <GLFW/glfw3.h>

//#include "world.h"
//#include "person.h"
//#include "object.h"
using namespace std;
class WORLD;

class OBJECT
{
public:
  /*int*/float x,xvel,y,yvel;
  int id;
  int layer;
  string type;
  bool exist;
  bool hostile;
  WORLD * world;
  OBJECT(WORLD&,int=0,int=0,int=0,int=0);
  OBJECT(int=0,int=0,int=0,int=0);
  void init(WORLD&,int=0,int=0,int=0,int=0);
  virtual void render();
  virtual bool logic(int);
  virtual void setText(string){};
  virtual void addToScore(){};
};
